# Prompt Patterns

## Business Framework Diagram

Use BLD or VTK.

Required prompt fields:

- Framework name
- Target audience
- 3-7 core components
- Direction of flow or hierarchy
- One dominant metaphor
- Exact labels
- Avoid clutter and long paragraphs

## Workflow / SOP

Use FBW or BLD.

Required prompt fields:

- Start state and end state
- 3-6 steps
- Actor or system for each step
- Arrows and checkpoints
- Output artifact at the end

## Article Cover

Use WEC, SBS, or ACD.

Required prompt fields:

- Headline
- Subheadline if needed
- Central metaphor
- Mood
- Text constraints
- Negative prompt for clutter and tiny text

## Before / After

Use ACD.

Required prompt fields:

- Left side pain / old way
- Right side outcome / new way
- Contrast colors
- One short headline
- Avoid exaggerated promises

## Prompt-Only Response Format

When the user asks for prompt only, return:

1. Selected style and reason.
2. Copy-ready prompt.
3. Optional revision notes if source content is ambiguous.
